[package]
name = "thecogumeta/goodpool"
description = "A simple, flexible, and lightweight object pooling module for Roblox that works with any type of object, not just Instances."
version = "1.1.0"
license = "MIT"
authors = ["Cogu (@thecogumeta)"]
realm = "shared"
registry = "https://github.com/UpliftGames/wally-index"
exclude = ["**"]
include = ["lib", "lib/*", "lib/**/*", "default.project.json", "wally.toml"]

[dependencies]

[dev-dependencies]
Specium = "thecogumeta/specium@1.1.1"